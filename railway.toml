[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "sh -c 'cd /app/server && npx prisma db push --skip-generate && node src/index.js'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
