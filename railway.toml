[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "npx prisma db push --schema=server/prisma/schema.prisma && npm run start --workspace=server"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
