[build]
builder = "NIXPACKS"
buildCommand = "npm ci && npm run build --workspace=client && npm run db:generate"

[deploy]
startCommand = "npm run start --workspace=server"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
